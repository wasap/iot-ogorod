{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "NODE_ENV=development nodemon index.js",
    "start": "node index",
    "cp": "cp ../config.js  ./config.js",
    "build": "gcloud builds submit --tag gcr.io/ogorod-289421/api",
    "deploy": "gcloud run deploy api --image gcr.io/ogorod-289421/api --platform managed --timeout 10 --memory 128",
    "all": "npm run cp && npm run build && npm run deploy"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/datastore": "^6.1.0",
    "@koa/cors": "^3.1.0",
    "@koa/router": "^8.0.8",
    "koa": "^2.11.0",
    "koa-bodyparser": "^4.3.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
